"""Mira — clean-rebuild application package (spec/00 charter §3).

This is the new-app namespace built alongside the legacy ``core/ ui/ data/``
packages. Nothing in here imports from the legacy app; the legacy app is the
oracle / parts-source / fallback until the §4-step-8 cutover.
"""
