"""Reusable base widgets + app-level UI infrastructure for the new UI.

Ported clean from the legacy ``ui/base/`` as the reassembly needs each piece, so the
new app is self-contained when the legacy ``ui/`` is archived (charter §4 step 8).
"""
