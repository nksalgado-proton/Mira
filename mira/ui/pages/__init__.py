"""Top-level page surfaces for the new UI, each bound only to the gateway.

Built one at a time as the reassembly moves downstream (ingest → events list → cull → …).
Unbuilt destinations show a :class:`~mira.ui.pages.placeholder.PlaceholderPage`.
"""
