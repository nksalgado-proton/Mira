"""The application shell — the persistent chrome around the page surfaces.

* :mod:`mira.ui.shell.main_window` — the QMainWindow + menu bar + page stack.
* :mod:`mira.ui.shell.page_stack` — the QStackedWidget by entry-key.
* :mod:`mira.ui.shell.sidebar` — the shared navigation-key constants the menu-
  bar dispatcher routes on.
"""
